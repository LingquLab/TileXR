diff --git a/src/comm/tilexr_internal.cpp b/src/comm/tilexr_internal.cpp
--- a/src/comm/tilexr_internal.cpp
+++ b/src/comm/tilexr_internal.cpp
@@ -38,6 +38,7 @@ const std::unordered_map<std::string, ChipName> CHIP_MAP = {
     {"Ascend950DT", ChipName::CHIP_950},
     {"Ascend950DT_9581", ChipName::CHIP_950},
     {"Ascend950DT_9582", ChipName::CHIP_950},
+    {"Ascend950DT_9584", ChipName::CHIP_950},
     {"Ascend950PR", ChipName::CHIP_950}
 };

diff --git a/tests/comm/unit/test_tilexr_source_guards.cpp b/tests/comm/unit/test_tilexr_source_guards.cpp
--- a/tests/comm/unit/test_tilexr_source_guards.cpp
+++ b/tests/comm/unit/test_tilexr_source_guards.cpp
@@ -124,6 +124,14 @@ void TestCommBuildIncludesProfilingHeaders()
     CheckContains(commPath, commText, "${ASCEND_HOME_PATH}/${ARCH}-linux/pkg_inc/profiling/");
 }

+void TestAscend950Dt9584ChipIsRecognized()
+{
+    const std::string path = "src/comm/tilexr_internal.cpp";
+    const auto text = ReadFile(path);
+
+    CheckContains(path, text, "{\"Ascend950DT_9584\", ChipName::CHIP_950}");
+}
+
 } // namespace

 int main()
@@ -136,6 +144,7 @@ int main()
     TestRuntimeEnvDoesNotPrependCannDevlib();
     TestRootCMakeRespectsAscendDriverOverride();
     TestCommBuildIncludesProfilingHeaders();
+    TestAscend950Dt9584ChipIsRecognized();

     if (g_failures != 0) {
         std::cerr << g_failures << " TileXR source guard checks failed" << std::endl;
